<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Work&amp;Control"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WC"
        tools:targetApi="31">
        <activity
            android:name=".DeleteRedactActivity"
            android:exported="false" />
        <activity
            android:name=".ShowWorkDaysDialog"
            android:exported="false" />
        <activity
            android:name=".AddWorkDay_Activity"
            android:exported="false" />
        <activity
            android:name=".ChangePeriod_Activity"
            android:exported="false" />
        <activity
            android:name=".AddSmena_Activity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>